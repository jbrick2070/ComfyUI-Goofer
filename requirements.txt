# ComfyUI-Goofer requirements
# All open-source, no API keys needed

cinemagoer>=2023.5.1      # IMDb data (goofs, metadata) — no API key required
pretty_midi>=0.2.10       # MIDI synthesis for background music
numpy>=1.24.0             # Array ops (already in ComfyUI)
torch>=2.0.0              # Tensor ops (already in ComfyUI)
Pillow>=10.0.0            # CPU frame generation (already in ComfyUI)
opencv-python>=4.8.0      # Optional: denoise/frame prep (graceful fallback)

# Prompt generation (Qwen2.5 mode) — models auto-download on first use
transformers>=4.37.0      # Qwen2.5-Instruct + MusicGen
accelerate>=0.26.0        # Required for Qwen2.5 float16 loading

# Models downloaded automatically — no manual steps needed:
#   Qwen/Qwen2.5-3B-Instruct          ~6 GB  (AI prompt generation, default)
#   Qwen/Qwen2.5-7B-Instruct         ~14 GB  (AI prompt generation, higher quality)
#   facebook/musicgen-large           ~3.3 GB (film score generation)
